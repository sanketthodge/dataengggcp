version: 2

models:
  - name: stg_orders
    description: Cleaned orders — casts amount to numeric and drops non-positive amounts.
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
      - name: amount
