version: 2

models:
  - name: mart_daily_sales
    description: Daily order count and revenue, by country.
    columns:
      - name: order_date
        tests:
          - not_null
      - name: orders
        tests:
          - not_null
      - name: revenue
        tests:
          - not_null
